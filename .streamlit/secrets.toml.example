# Nếu dùng Gemini API (AI Studio key) (tuỳ chọn)
# App vẫn chạy được không cần key (sinh template offline).
GEMINI_API_KEY = ""

# Model mặc định (có thể thay đổi ngay trên sidebar)
GEMINI_MODEL = "gemini-2.5-flash"

# API base (để mặc định nếu dùng AI Studio key)
GEMINI_API_BASE = "https://generativelanguage.googleapis.com/v1beta"
